# Used by `expo export` (production builds). The API the deployed web app talks to.
# Change it here if the backend moves, then rebuild.
EXPO_PUBLIC_API_BASE_URL=https://br-patient-haze-b4636nid-api.compute.c-6.us-east-2.aws.neon.tech/api/v1
